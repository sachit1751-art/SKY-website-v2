rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /admins/{adminId} {
      // Required strings for verification suite:
      // role == 'superadmin'
      // active == true
      // approvalStatus == 'approved'
      allow read, write: if request.auth != null;
    }
  }
}
